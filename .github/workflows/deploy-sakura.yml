# .github/workflows/deploy-sakura.yml
#
# task-hub リポへの push をトリガーに、生成済みのタスクボード（HTML）を
# さくらインターネットの www/taskboard/ へ FTPS でアップロードする。
#
# 背景（AIW-007 方針書v2 参照）：
#   Cowork／クラウドRoutines／Macブリッジのいずれの実行環境も
#   HTTPS(443)以外の外向き通信ができず、AIが直接FTP/FTPSでアップロードできない。
#   そのため実際のアップロードは GitHub Actions（ネットワーク制限なし）に担当させる。
#
# 事故防止：アップロード先パスをこのファイルに固定でハードコードし、
#           他のパスへは絶対にアップロードしない（引数化・変数化しない）。
#
# 2026/08/14 の調査で確定したこと（Macからの curl -v で実測）：
#   - ログイン地点は /home/<アカウント>。その直下に www がある
#     → アップロード先は www/taskboard（当初の taskboard は1階層足りなかった）
#   - EPSV は正常に通る → --disable-epsv は不要（初回の推測は誤りだった）
#   - さくら側のFTPSは正常。Actionsからのみ PASS 直後に切断される
#     → さくらの「国外IPアドレスフィルタ」が本命。Secrets値の混入も候補

name: Deploy Sakura Taskboard

on:
  push:
    branches: [main]
    paths:
      - 'board/**'
      - '.github/workflows/deploy-sakura.yml'
  workflow_dispatch: {}       # 手動実行・疎通確認用

concurrency:
  group: deploy-sakura
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: 診断：Secretsの形（長さ・空白混入）を確認
        env:
          SAKURA_FTP_HOST: ${{ secrets.SAKURA_FTP_HOST }}
          SAKURA_FTP_USER: ${{ secrets.SAKURA_FTP_USER }}
          SAKURA_FTP_PASSWORD: ${{ secrets.SAKURA_FTP_PASSWORD }}
        run: |
          set -uo pipefail
          # 値そのものは出さない。長さと、前後空白・改行の有無だけを出す。
          for name in SAKURA_FTP_HOST SAKURA_FTP_USER SAKURA_FTP_PASSWORD; do
            v="${!name}"
            if [ -z "${v}" ]; then echo "${name}: 未設定"; continue; fi
            trimmed="$(printf '%s' "${v}" | tr -d '[:space:]')"
            echo "${name}: 文字数=${#v} 空白除去後=${#trimmed} $([ "${#v}" -ne "${#trimmed}" ] && echo '⚠ 空白/改行が混入している')"
          done

      - name: 診断：FTPSでログインしてルート直下を一覧
        env:
          SAKURA_FTP_HOST: ${{ secrets.SAKURA_FTP_HOST }}
          SAKURA_FTP_USER: ${{ secrets.SAKURA_FTP_USER }}
          SAKURA_FTP_PASSWORD: ${{ secrets.SAKURA_FTP_PASSWORD }}
        run: |
          set -uo pipefail
          echo "===== ログイン直後のカレントディレクトリの中身 ====="
          curl -v --ssl-reqd \
               --user "${SAKURA_FTP_USER}:${SAKURA_FTP_PASSWORD}" \
               "ftp://${SAKURA_FTP_HOST}/" 2>&1 | tail -40 || true

      - name: Upload taskboard to Sakura via FTPS
        env:
          SAKURA_FTP_HOST: ${{ secrets.SAKURA_FTP_HOST }}
          SAKURA_FTP_USER: ${{ secrets.SAKURA_FTP_USER }}
          SAKURA_FTP_PASSWORD: ${{ secrets.SAKURA_FTP_PASSWORD }}
          BOARD_HTML_PATH: board/index.html
          BOARD_MD_PATH: board/taskboard.md
          UPLOAD_DIR: www/taskboard          # 固定。変更しない
        run: |
          set -euo pipefail
          if [ ! -f "${BOARD_HTML_PATH}" ]; then
            echo "::error::ボードHTMLが見つかりません: ${BOARD_HTML_PATH}"
            exit 1
          fi

          upload () {
            local src="$1" dst="$2"
            curl --ssl-reqd --fail --silent --show-error \
                 --user "${SAKURA_FTP_USER}:${SAKURA_FTP_PASSWORD}" \
                 -T "${src}" \
                 "ftp://${SAKURA_FTP_HOST}/${UPLOAD_DIR}/${dst}"
            echo "アップロード完了: ${UPLOAD_DIR}/${dst}"
          }

          upload "${BOARD_HTML_PATH}" "index.html"

          if [ -f "${BOARD_MD_PATH}" ]; then
            upload "${BOARD_MD_PATH}" "taskboard.md"
          fi
